import { styled } from '@mui/material/styles';

export const TaskListContainer = styled('div')({
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    fontFamily: 'Arial',
});